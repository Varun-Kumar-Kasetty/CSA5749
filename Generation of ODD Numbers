#include<stdio.h>
int main()
{
	int i = 1, n;
	printf("Enter the value of n = ");
	scanf("%d", &n);
	printf("The ODD Numbers are = ");
	while (i<=n)
	{
		printf(", %d", i);
		i = i + 2;
	}
	return 0;
}
